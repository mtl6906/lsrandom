project(lsRandom)

cmake_minimum_required(VERSION 3.10)

aux_source_directory(src/ls SRC)
aux_source_directory(sample SRC_SAMPLE)

include_directories(src)

add_executable(testcase ${SRC} ${SRC_SAMPLE})
add_library(lsrandom SHARED ${SRC})
